# =============================================================================
# CropBid — https://cropbid.in
# =============================================================================
# Everything public is open to every crawler, including the AI/answer engines.
# Being quoted in an AI answer is worth more to us than the page view we lose,
# so nothing here is blocked on "training data" grounds.
#
# NOTE ON THE REPEATED Disallow BLOCKS BELOW: a crawler that finds its own
# User-agent line ignores the `*` group entirely. So the private-route list has
# to be repeated in every named group — it is not redundant, and dropping it
# from one group opens the whole dashboard to that crawler.
# =============================================================================

User-agent: *
Allow: /
# Signed-in surfaces — nothing here renders without a session, so a crawler
# only ever gets a redirect to /login. Cheaper for everyone to skip them.
Disallow: /admin
Disallow: /farmer
Disallow: /buyer
Disallow: /settings
Disallow: /agent
Disallow: /negotiations
Disallow: /transactions
Disallow: /auctions
Disallow: /logistics
Disallow: /listings
Disallow: /onboarding
Disallow: /forgot-password
Disallow: /reset-password
Disallow: /api/
# Query-string noise: the storefront's client-side search rewrites ?q= and would
# otherwise generate unlimited near-duplicate URLs.
Disallow: /*?q=
Disallow: /*?search=

# --- AI answer engines & assistants -----------------------------------------
# Explicitly welcomed. These are the crawlers behind ChatGPT, Perplexity,
# Claude, Gemini and Copilot answers.
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: ClaudeBot
User-agent: Claude-User
User-agent: Claude-SearchBot
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: Amazonbot
User-agent: Meta-ExternalAgent
User-agent: DuckAssistBot
User-agent: CCBot
User-agent: cohere-ai
Allow: /
Disallow: /admin
Disallow: /farmer
Disallow: /buyer
Disallow: /settings
Disallow: /agent
Disallow: /negotiations
Disallow: /transactions
Disallow: /auctions
Disallow: /logistics
Disallow: /listings
Disallow: /onboarding
Disallow: /forgot-password
Disallow: /reset-password
Disallow: /api/

Sitemap: https://cropbid.in/sitemap.xml
